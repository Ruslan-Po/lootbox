export let balance     = 300;
export let selectedIdx = 0;
export let isAnimating = false;

export function setBalance(v)     { balance     = v; }
export function setSelectedIdx(v) { selectedIdx = v; }
export function setIsAnimating(v) { isAnimating = v; }
